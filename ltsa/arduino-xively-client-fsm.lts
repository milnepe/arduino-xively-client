XIVELY_FSM = IDLE,
IDLE = (idle->IDLE
		|sample->SAMPLING
		|receive->RECEIVING
		|connect->CONNECTING),
SAMPLING = (idle->IDLE),
CONNECTING =(disconnect->IDLE
			|fail->FAILING),
RECEIVING = (disconnect->IDLE),
FAILING = (disconnect->IDLE).
